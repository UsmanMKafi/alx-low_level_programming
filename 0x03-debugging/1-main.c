#include <stdio.h>

/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	print("infinite loop incoming :(\n");

	i = 0;
	/**
	 * while (i < 0)
	 * {
	 * 	putchar(i);
	 * }
	 */
	printf("infinite loop stopped! \\0/\n");

	return (0);
}
